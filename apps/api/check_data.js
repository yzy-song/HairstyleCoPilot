const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function checkData() { const consultations = await prisma.consultation.findMany({ select: { id: true, status: true } }); const templates = await prisma.hairstyleTemplate.findMany({ select: { id: true, name: true, modelKey: true } }); console.log('Consultations:', consultations); console.log('Templates:', templates); await prisma.\(); } checkData();
